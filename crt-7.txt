#write a python program to read age as input from the user and check whether the age is vote or not
age=int(input("Enter the age:"))
if(age>=18):
    print("You are eligible to vote.")
else:
    print("You are not eligible to vote")